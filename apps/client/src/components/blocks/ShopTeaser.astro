---
/**
 * `block:shop-teaser` (spec 05) — registered now so `/admin` can list it, and
 * empty until spec 11 wires three products from Shopify. It renders nothing,
 * so switching it on before then leaves no gap. Hidden by default.
 */
interface Props {
  copy?: unknown;
  flags?: unknown;
}
---
